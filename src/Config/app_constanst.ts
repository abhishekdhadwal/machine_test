
const seckret_keys = {
      admin_seckret_key : "admin_seckret_key"
}

const scope = {
      admin : "admin"
}

const default_limit = 10;
const default_otp = 1234;

export {
      seckret_keys,
      scope,
      default_limit,
      default_otp
}


